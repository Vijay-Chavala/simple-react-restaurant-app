import React from "react";

const Reservation = () => {
  return (
    <div
      style={{
        background: "url(images/ReservationRestaurant.jpg)",
        backgroundPosition: "center",
        backgroundSize: "cover",
        backgroundRepeat: "no-repeat",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "90vh",
      }}
    >
      <h1 style={{ fontSize: "3rem ", color: "#fff" }}>Reservation Page</h1>
    </div>
  );
};

export default Reservation;
